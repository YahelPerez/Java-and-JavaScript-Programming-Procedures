# =====================================================
# MySQL Database Configuration
# Project: Java and JavaScript Programming Procedures
# Author: YahelPerez
# Date: November 11, 2025
# =====================================================

# Database Connection Details
DB_HOST=localhost
DB_PORT=3306
DB_NAME=reservation_graph_system
DB_USER=reservation_app
DB_PASSWORD=SecurePass123!

# Connection Pool Settings (for applications)
DB_POOL_MIN_SIZE=5
DB_POOL_MAX_SIZE=20
DB_POOL_ACQUIRE_INCREMENT=2
DB_POOL_MAX_IDLE_TIME=300

# MySQL Specific Settings
DB_SSL_MODE=DISABLED
DB_CHARSET=utf8mb4
DB_TIMEZONE=America/Mexico_City

# Application Database URLs
# For Java/Spring Boot
SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/reservation_graph_system?useSSL=false&serverTimezone=America/Mexico_City&characterEncoding=utf8mb4
SPRING_DATASOURCE_USERNAME=reservation_app
SPRING_DATASOURCE_PASSWORD=SecurePass123!

# For Node.js/Sequelize
NODE_DATABASE_URL=mysql://reservation_app:SecurePass123!@localhost:3306/reservation_graph_system

# Admin Connection (for setup only)
ADMIN_DB_USER=root
ADMIN_DB_PASSWORD=your_root_password_here